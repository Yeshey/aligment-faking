# ai aligment

1. Run with `sbatch run.slurm`
2. Know that one job running non stop for 8h gets killed
3. `squeue -u joaofilipe.silvade`
4. `tail -f logs/slurm-JOBID.log logs/slurm-JOBID.err`
5. cancel with `scancel <JOB ID>`
